<%- include("partials/header.ejs") %>
    <%- include("partials/navbar.ejs") %>

        <body>


            <section class="container">
                <h1> Hello Every One I Have guesed what you requested here: </h1>
                <% if(sex=="Female" ){ %>
                    <h1>She is called : <%= name %>
                    </h1>
                    <h2>She is <%= ages %> years old</h2>
                    <div>
                        <h2>In short she said that:</h2>
                        <p>
                            <%= description %>
                    </div>
                    </p>
                    <hr>
                    <%for(let post of posts){ %>
                        <h1>She has the posts About : </h1>
                        <h2>The title : <%= post.title %>
                        </h2>
                        <h3>Is Writen by : <%= post.author %>
                        </h3>
                        <% if(post.img){ %>

                            <img src="<%= post.img %>" style="width:100%; height: 80%;" alt="">
                            <% } %>
                                <% } %>
                                    <% }else{ %>
                                        <h1>He is called : <%= name %>
                                        </h1>
                                        <h2>He is <%= ages %> years old</h2>
                                        <div>
                                            <h2>In short He said that:</h2>
                                            <p>
                                                <%= description %>
                                        </div>
                                        <hr>
                                        <%for(let post of posts){ %>
                                            <h1>He has the posts About : </h1>
                                            <h2>The title : <%= post.title %>
                                            </h2>
                                            <h3>Is Writen by : <%= post.author %>
                                            </h3>
                                            <% if(post.img){ %>

                                                <img src="<%= post.img %>" style="width:100%; height: 80%;" alt="">
                                                <% } %>
                                                    <% }%>
                                                        <% } %>

            </section>
        </body>

        <%- include("partials/footer.ejs") %>