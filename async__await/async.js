// const login = async (username, password) => {
//   if (!username || !password) throw "invalid credentials";
//   if (password === "muheto") return "Welcome";
//   throw "invalid password";
// };

// login("hodal")
//   .then((answer) => {
//     console.log(answer, "again");
//   })
//   .catch((err) => console.log("Invalid"));

const myNames = async () => {
  return "LALALAL al";
};
myNames()
  .then((data) => {
    console.log("our result shows that you write : ", data);
  })
  .catch((error) => {
    console.log("Ntibibaho");
  });
